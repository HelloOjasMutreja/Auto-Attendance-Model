<%= form_with(model: class_section) do |form| %>
  <% if class_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(class_section.errors.count, "error") %> prohibited this class_section from being saved:</h2>

      <ul>
        <% class_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.select :user_id, User.all.map { |u| [u.first_name, u.id] }, { prompt: "Select User" } %>
  </div>

  <div>
    <%= form.label :attendance_id, style: "display: block" %>
    <%= form.text_field :attendance_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
